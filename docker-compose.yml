version: '3'
services:
  backend:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - ./server:/app
  frontend:
    build: ./client
    ports:
      - "3000:80"
    volumes:
      - ./client:/app